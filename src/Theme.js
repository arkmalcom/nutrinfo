import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
      primary: {
        main: "#E8CEBF",
        light: "#FDF8F5",
        dark: "#DDAF94",
      },
      secondary: {
        main: "#266150",
      },
    },
  })

export default theme;
